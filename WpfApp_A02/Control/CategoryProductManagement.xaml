<UserControl x:Class="WpfApp_A02.Control.CategoryProductManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp_A02.Control" 
             xmlns:local1="clr-namespace:WpfApp_A02.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Background="#f5f5f5" Margin="10">
       
        <Grid Background="#f9f9f9" Margin="20">
            <TabControl FontSize="16" Padding="10" Background="White">

                <!-- ✅ CATEGORY TAB -->
                <TabItem Header="📁 Categories">
                    <Grid Background="#E8F0FE">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="Add Category" Width="120" Margin="0,0,10,0" Click="BtnAddCategory_Click"/>
                            <Button Content="Edit Selected" Width="120" Margin="0,0,10,0" Click="BtnEditCategory_Click"/>
                            <Button Content="Delete Selected" Width="120" Click="BtnDeleteCategory_Click"/>
                        </StackPanel>

                        <DataGrid x:Name="dgCategories" Grid.Row="1" AutoGenerateColumns="False"
                                  CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="Column" Margin="0,10,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="60"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding CategoryName}" Width="*"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                                <DataGridTemplateColumn Header="Picture" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Image Width="50" Height="50" Stretch="Uniform"
                                          Loaded="Image_Loaded"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- ✅ PRODUCT TAB -->
                <TabItem Header="📦 Products">
                    <Grid Background="#FFFDE7">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="Add Product" Width="120" Margin="0,0,10,0" Click="BtnAddProduct_Click"/>
                            <Button Content="Edit Selected" Width="120" Margin="0,0,10,0" Click="BtnEditProduct_Click"/>
                            <Button Content="Delete Selected" Width="120" Click="BtnDeleteProduct_Click"/>
                        </StackPanel>

                        <DataGrid x:Name="dgProducts" Grid.Row="1" AutoGenerateColumns="False"
                                  CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="Column" Margin="0,10,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" Width="60"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="Supplier ID" Binding="{Binding SupplierId}" Width="80"/>
                                <DataGridTextColumn Header="Category ID" Binding="{Binding CategoryId}" Width="80"/>
                                <DataGridTextColumn Header="Quantity/Unit" Binding="{Binding QuantityPerUnit}" Width="120"/>
                                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice}" Width="100"/>
                                <DataGridTextColumn Header="In Stock" Binding="{Binding UnitsInStock}" Width="80"/>
                                <DataGridTextColumn Header="On Order" Binding="{Binding UnitsOnOrder}" Width="80"/>
                                <DataGridTextColumn Header="Reorder Level" Binding="{Binding ReorderLevel}" Width="100"/>
                                <DataGridCheckBoxColumn Header="Discontinued" Binding="{Binding Discontinued}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

            </TabControl>
        </Grid>
    </Grid>

</UserControl>
