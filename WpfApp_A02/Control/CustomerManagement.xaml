<UserControl x:Class="WpfApp_A02.Control.CustomerManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp_A02.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Tìm khách hàng:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtSearch" Width="200"/>
            <Button Content="Tìm" Click="BtnSearch_Click" Margin="10,0,0,0"/>
            <Button Content="Clear" Click="BtnClear_Click" Margin="5,0,0,0"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid 
            x:Name="CustomerDataGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            SelectionChanged="CustomerDataGrid_SelectionChanged"
            SelectedItem="{Binding SelectedCustomer}"
            Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="Auto"/>
                <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="*"/>
                <DataGridTextColumn Header="Contact Name" Binding="{Binding ContactName}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Thêm" Click="BtnAdd_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Sửa" Click="BtnUpdate_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Xóa" Click="BtnDelete_Click" Width="80"/>
        </StackPanel>
    </Grid>

</UserControl>
